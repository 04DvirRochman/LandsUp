export async function getUserSubsFromDB(userId) {
    let table = [
        { id: 'hrteshgte', name: 'LOT921', origin: 'Warsaw', destination: 'Tel Aviv', departuretime: '7:00 20.11', arrivaltime: "20:00 5.11" ,airline: "Taiwan Airlines", terminal:9}
    ];
    return table;
};